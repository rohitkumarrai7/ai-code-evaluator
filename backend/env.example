# Database (Replace with your actual Railway credentials in local .env)
DATABASE_URL="mysql://root:<your-password>@containers-us-west-2.railway.app:3306/railway"

# Gemini API key (Replace with your actual key in local .env)
GEMINI_API_KEY="your-gemini-api-key-here"

# Server settings
PORT=3001
NODE_ENV=development

# Frontend URL (set to local dev or production frontend)
FRONTEND_URL="http://localhost:3000"
